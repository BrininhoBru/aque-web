---
phase: 01-api-config-type-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
autonomous: true
requirements: [DEBT-01]
closes_issues: [5]
files_modified:
  - src/app/core/services/transaction.service.ts
  - src/app/core/services/category.service.ts
  - src/app/core/services/person.service.ts
  - src/app/core/services/recurring.service.ts
  - src/app/core/services/split.service.ts
  - src/app/core/services/dashboard.service.ts
  - src/app/core/auth/auth.interceptor.spec.ts
  - src/app/features/dashboard/dashboard.component.spec.ts
  - src/app/features/split/split.component.spec.ts

estimate:
  tokens: 34000
  raw_tokens: 17000
  tasks: 3
  confidence: low   # 0 completed plans in this project -> 0 calibration samples

must_haves:
  truths:
    - "Every HTTP call in the app derives its URL from one configured base value, not from a per-file literal (DEBT-01, success criterion 2)"
    - "No file anywhere under src/app/ contains a quoted API-path literal, comments included"
    - "Every spec that asserts a request URL builds it from the same configured value, so specs and sources cannot drift"
    - "The app still builds and all 10 pre-existing specs still pass (success criterion 5)"
  artifacts:
    - src/app/core/services/transaction.service.ts
    - src/app/core/services/category.service.ts
    - src/app/core/services/person.service.ts
    - src/app/core/services/recurring.service.ts
    - src/app/core/services/split.service.ts
    - src/app/core/services/dashboard.service.ts
  key_links:
    - "All 7 HTTP call sites (6 domain services here + auth login from 01-01) import environment from src/environments/environment"
    - "4 existing spec files (auth.service from 01-01, plus auth.interceptor, dashboard.component, split.component here) build their expected URLs from the same environment value"
---

<objective>
Repeat the config path proven in 01-01 across every remaining HTTP call site and every remaining spec that asserts a request URL.

Purpose: DEBT-01 (GH #5), success criterion 2. Plan 01-01 created `environment.apiBaseUrl`, wired the production file replacement, and migrated one call site. A partial migration leaves the base path split across two sources of truth — which is the exact defect being closed — so this plan finishes it and carries the whole-tree gate.

Output: 6 migrated domain services, 3 migrated spec files, and a green full test suite with no quoted API-path literal left anywhere under `src/app/`.

Non-goal: this plan changes ZERO rendered output and ZERO request destinations — `apiBaseUrl` holds the same value the literals held. No template, style, layout, copy, or interaction behavior may change. If a diff touches an `.html` file or a `styles:` array, it is out of scope.

Closes GH [#5](https://github.com/BrininhoBru/aque-web/issues/5) on completion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@.planning/phases/01-api-config-type-cleanup/01-01-SUMMARY.md
</context>

<interfaces>

## Artifacts this plan consumes

| Symbol | Kind | File | Shape |
|--------|------|------|-------|
| `environment` | exported const | `src/environments/environment.ts` (created by 01-01) | `{ apiBaseUrl: string }` |

Import specifier from every file in this plan: `../../../environments/environment` — all nine files sit exactly three levels below `src/` (`src/app/core/services/`, `src/app/core/auth/`, `src/app/features/<feature>/`). The repo has no path aliases; see CONVENTIONS.md "Path Aliases: None configured". 01-01-SUMMARY.md records the exact specifier it used — match it.

## Artifacts this plan produces

No new exported symbol. Each of the six services keeps its existing `private readonly base` field; only the field's right-hand side changes from a literal to a template literal. Phase 3 writes new specs against these same `base` values.

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate transaction, category and person services to the configured base</name>
  <files>src/app/core/services/transaction.service.ts, src/app/core/services/category.service.ts, src/app/core/services/person.service.ts</files>
  <precondition>`src/environments/environment.ts` exists and exports a const named `environment` with an `apiBaseUrl` member (produced by plan 01-01). If it does not, halt — this plan cannot run before 01-01.</precondition>
  <read_first>
    - `src/environments/environment.ts` (created in 01-01 — confirm the export name)
    - `src/app/core/services/transaction.service.ts` (line 28)
    - `src/app/core/services/category.service.ts` (line 9)
    - `src/app/core/services/person.service.ts` (line 9)
  </read_first>
  <action>
Continues DEBT-01 (GH #5), success criterion 2. Each of these three services declares a `private readonly base = <literal>` field. In each one: add `import { environment } from '../../../environments/environment';` alongside the existing imports, and rewrite the `base` field as a template literal of `environment.apiBaseUrl` plus that service's resource suffix.

Keep the field declaration exactly as it is — `private readonly base` — and keep every method body untouched. Only the field's right-hand side changes.

| File | Line | Resource suffix to append |
|------|------|---------------------------|
| `src/app/core/services/transaction.service.ts` | 28 | `/transactions` |
| `src/app/core/services/category.service.ts` | 9 | `/categories` |
| `src/app/core/services/person.service.ts` | 9 | `/persons` |

The resulting URLs are byte-identical to the ones the literals produced, so no spec expectation changes as a result of this task.

Do not leave a removed literal behind in a code comment — Task 3's acceptance gate greps the whole `src/app/` tree and does not exempt comments. Do not touch `proxy.conf.json` or `nginx.conf`; both continue to serve the same path.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && for f in src/app/core/services/transaction.service.ts src/app/core/services/category.service.ts src/app/core/services/person.service.ts; do grep -q 'environments/environment' "$f" || { echo "missing import: $f"; exit 1; }; grep -qE "['\"\`]/api" "$f" && { echo "literal survives: $f"; exit 1; }; done; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three files imports the environment module and contains no quoted API-path literal — the loop in `<verify>` exits 0
    - Each file still declares `private readonly base`: `for f in src/app/core/services/transaction.service.ts src/app/core/services/category.service.ts src/app/core/services/person.service.ts; do grep -q 'private readonly base' "$f" || exit 1; done` exits 0
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 and `npm run build` exits 0
    - `git diff --name-only` lists no `.html` file for this task
  </acceptance_criteria>
  <done>The transaction, category and person services build their base URL from `environment.apiBaseUrl`, with typecheck and build green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate recurring, split and dashboard services to the configured base</name>
  <files>src/app/core/services/recurring.service.ts, src/app/core/services/split.service.ts, src/app/core/services/dashboard.service.ts</files>
  <read_first>
    - `src/app/core/services/transaction.service.ts` (migrated in Task 1 — copy its import line and field shape verbatim)
    - `src/app/core/services/recurring.service.ts` (line 16)
    - `src/app/core/services/split.service.ts` (line 13)
    - `src/app/core/services/dashboard.service.ts` (line 9)
  </read_first>
  <action>
Same mechanical edit as Task 1, applied to the remaining three domain services. Split across two tasks only to keep each task within the five-file limit; the transformation is identical.

In each file: add `import { environment } from '../../../environments/environment';` alongside the existing imports, and rewrite the `private readonly base` field as a template literal of `environment.apiBaseUrl` plus that service's resource suffix. Field declaration and every method body stay untouched.

| File | Line | Resource suffix to append |
|------|------|---------------------------|
| `src/app/core/services/recurring.service.ts` | 16 | `/recurring` |
| `src/app/core/services/split.service.ts` | 13 | `/split` |
| `src/app/core/services/dashboard.service.ts` | 9 | `/dashboard` |

After this task all seven HTTP call sites in the app (these six plus `auth.service.ts` from 01-01) derive their URL from the one configured value; the spec files follow in Task 3.

Do not leave a removed literal behind in a code comment — Task 3's acceptance gate greps the whole `src/app/` tree and does not exempt comments.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && for f in src/app/core/services/transaction.service.ts src/app/core/services/category.service.ts src/app/core/services/person.service.ts src/app/core/services/recurring.service.ts src/app/core/services/split.service.ts src/app/core/services/dashboard.service.ts src/app/core/auth/auth.service.ts; do grep -q 'environments/environment' "$f" || { echo "missing import: $f"; exit 1; }; done; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 7 call sites import the environment module — the loop in `<verify>` (6 domain services plus `auth.service.ts`) exits 0. Equivalent one-liner if a count is preferred: `grep -l 'environments/environment' src/app/core/services/{transaction,category,person,recurring,split,dashboard}.service.ts src/app/core/auth/auth.service.ts | wc -l` returns 7
    - No source file under `src/app/` contains a quoted API-path literal: `grep -rlE "['\"\`]/api" src/app/ --include='*.service.ts'` returns no matches (exit code 1)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 and `npm run build` exits 0
    - `git diff --name-only` lists no `.html` file for this task
  </acceptance_criteria>
  <done>All six domain services plus the auth login call derive their URL from `environment.apiBaseUrl`, with typecheck and build green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the three remaining specs and close the whole-tree gate</name>
  <files>src/app/core/auth/auth.interceptor.spec.ts, src/app/features/dashboard/dashboard.component.spec.ts, src/app/features/split/split.component.spec.ts</files>
  <read_first>
    - `src/app/core/auth/auth.service.spec.ts` (migrated in 01-01 — copy its import line and URL construction verbatim)
    - `src/app/core/auth/auth.interceptor.spec.ts` (lines 50, 52, 58, 60, 82, 85, 108, 111)
    - `src/app/features/dashboard/dashboard.component.spec.ts` (line 64)
    - `src/app/features/split/split.component.spec.ts` (line 109)
  </read_first>
  <action>
Completes DEBT-01 (GH #5), success criterion 2. These three specs still carry their own path literals; migrating them means a deploy-time change to `apiBaseUrl` cannot make specs and sources disagree. This also pre-satisfies Phase 3's requirement that spec URL expectations derive from the configured value.

- `src/app/core/auth/auth.interceptor.spec.ts` — import from `../../../environments/environment`; rebuild the arbitrary request URLs at lines 50/52/58/60 (categories resource) and 82/85/108/111 (transactions resource) from `environment.apiBaseUrl`. These URLs exist only to exercise the interceptor; keep the same resource names so the existing assertions stay meaningful.
- `src/app/features/dashboard/dashboard.component.spec.ts` — import from `../../../environments/environment`; rewrite the local `base` constant at line 64 as a template literal of `environment.apiBaseUrl` plus `/dashboard`. Every `expectOne` in that file already interpolates `base`, so no other line changes.
- `src/app/features/split/split.component.spec.ts` — import from `../../../environments/environment`; rewrite the `r.url.startsWith(...)` argument at line 109 as a template literal of `environment.apiBaseUrl` plus `/split/`.

No spec is added, deleted or skipped: the total spec count after this task equals the count before plan 01-01 started.

Do not leave a removed literal behind in a code comment — this task's acceptance gate greps the whole `src/app/` tree and does not exempt comments.
  </action>
  <verify>
    <automated>! grep -rqE "['\"\`]/api" src/app/ && npm run build && npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
    - No file anywhere under `src/app/` contains a quoted API-path literal: `grep -rnE "['\"\`]/api" src/app/` returns no matches (exit code 1). The definition in `src/environments/` is outside this tree and is expected to survive.
    - All three spec files import the environment module: `for f in src/app/core/auth/auth.interceptor.spec.ts src/app/features/dashboard/dashboard.component.spec.ts src/app/features/split/split.component.spec.ts; do grep -q 'environments/environment' "$f" || exit 1; done` exits 0
    - `npm run build` exits 0
    - `npm test -- --watch=false` reports 0 failures across the 10 pre-existing spec files, with the spec count unchanged (success criterion 5)
    - `git diff --name-only` lists no `.html` file and no change under `public/` — this plan alters zero rendered output
  </acceptance_criteria>
  <done>Every HTTP call site and every spec expectation in the app derives its URL from `environment.apiBaseUrl`, with the build and the full suite green and no path literal left under `src/app/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser SPA -> `aque-backend` REST API | Every request crosses here carrying the `Authorization: Bearer` header injected by `authInterceptor`. This plan changes how the destination URL is *constructed* for the six remaining call sites, making `apiBaseUrl` the single input governing where authenticated requests go. |

The build-pipeline boundary and the environment files themselves are owned by plan 01-01 (threats T-01-01, T-01-02); this plan adds no member to the environment object and does not touch `angular.json`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | `base` field of the 6 domain services | low | accept | The six `base` fields become derived values rather than literals, so a single compromised `apiBaseUrl` would now redirect all six services at once. The blast radius is unchanged in practice — a build-time-only value that already governs the auth call is the same trust root — and the runtime-lookup ban that protects it is enforced in 01-01 (T-01-01). Destinations are provably unchanged here: `apiBaseUrl` holds the same value the literals held, and the full pre-existing suite must stay green (Task 3). |
| T-01-SC | Tampering | npm installs | high | accept | No package-manager install task exists in this plan — no dependency is added, removed or upgraded, so the supply-chain surface is unchanged and no package legitimacy audit is required. Enforced by acceptance gate: `git diff --exit-code package.json package-lock.json` must exit 0 (see `<verification>`). If a task turns out to need a new dependency, stop and route back to planning for a legitimacy audit rather than installing inline. |

ASVS level 1, block-on threshold `high`. This plan carries no unmitigated `high` threat: T-01-SC is `high` by category but its disposition is `accept` on the grounds that the activity does not occur, with an automated gate proving it.
</threat_model>

<verification>
Run from the repo root after all three tasks:

1. `npm run build` — exits 0 (production configuration, so `environment.prod.ts` is the module actually compiled).
2. `npm test -- --watch=false` — 0 failures across the 10 pre-existing spec files. No spec is added, deleted or skipped in this plan; the count must be unchanged.
3. `grep -rnE "['\"\`]/api" src/app/` — no matches.
4. `git diff --exit-code package.json package-lock.json` — exits 0 (T-01-SC gate: no dependency changed).
5. `git diff --name-only` — contains no `.html` file and nothing under `public/` or `src/styles.css`. This plan produces zero rendered-output change.
</verification>

<success_criteria>
Maps to ROADMAP.md Phase 1:

2. No file under `src/app/` contains a hardcoded API-path string literal; all 6 domain services and `auth.service.ts`'s login call derive their URL from `environment.apiBaseUrl` — proven by verification step 3 plus Task 2's 7-file import loop.
5. `npm run build` succeeds and `npm test` passes with the existing 10 specs still green — verification steps 1 and 2.

Closes GH [#5](https://github.com/BrininhoBru/aque-web/issues/5).
</success_criteria>

<output>
Create `.planning/phases/01-api-config-type-cleanup/01-02-SUMMARY.md` when done.

Record in the summary: the final `base` expression for each of the six services (Phase 3 writes specs asserting these URLs) and confirmation that the whole-tree literal gate passes.
</output>
