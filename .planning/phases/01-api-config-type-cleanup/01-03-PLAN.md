---
phase: 01-api-config-type-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DEBT-04, DEBT-05]
closes_issues: [9, 10]
files_modified:
  - src/app/features/login/login.component.ts
  - src/app/features/dashboard/dashboard.component.ts

estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 2
  confidence: low   # 0 completed plans in this project -> 0 calibration samples

must_haves:
  truths:
    - "The login form's Signal Forms schema callback is typed against the login model shape (DEBT-04, success criterion 3)"
    - "The dashboard donut total formatter is typed against the ApexCharts option type (DEBT-05, success criterion 4)"
    - "Neither file reaches strictness by suppression: no @ts-ignore, no @ts-expect-error, no widening cast"
    - "The app still builds and the pre-existing dashboard spec still passes (success criterion 5)"
  artifacts:
    - src/app/features/login/login.component.ts
    - src/app/features/dashboard/dashboard.component.ts
  key_links:
    - "LoginModel types both the model signal and the form() schema callback parameter, so the two cannot drift"
    - "DonutTotalContext mirrors the apexcharts formatter declaration at node_modules/apexcharts/types/apexcharts.d.ts:996"
---

<objective>
Give the project's two remaining loosely-typed callbacks their real types.

Purpose: DEBT-04 (GH #9) and DEBT-05 (GH #10) — two callbacks are annotated with the TypeScript top type, defeating the strict-mode guarantees the rest of the codebase relies on. Both are single-line type corrections against shapes the installed libraries already declare.

Output: an exported `LoginModel` interface and a typed Signal Forms schema callback; an exported `DonutTotalContext` type alias and a typed ApexCharts donut formatter.

This plan is fully independent of the `environment.ts` migration in plans 01-01 and 01-02: different files, no shared state, no new dependency. It runs in Wave 1 alongside 01-01.

Non-goal: this plan changes ZERO rendered output and ZERO runtime behavior. No template, style, layout, copy, or interaction behavior may change. If a diff touches an `.html` file or a `styles:` array, it is out of scope.

Closes GH [#9](https://github.com/BrininhoBru/aque-web/issues/9) and [#10](https://github.com/BrininhoBru/aque-web/issues/10).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
</context>

<interfaces>

## Artifacts this plan produces

| Symbol | Kind | File | Shape |
|--------|------|------|-------|
| `LoginModel` | exported interface | `src/app/features/login/login.component.ts` | `{ username: string; password: string }` |
| `DonutTotalContext` | exported type alias | `src/app/features/dashboard/dashboard.component.ts` | `ApexCharts.ApexOptions & { globals: { seriesTotals: number[] } }` |

Phase 3 writes `login.component.spec.ts` against `LoginModel` — do not rename it.

## Types verified against the installed libraries (do not substitute)

Both signatures below were type-checked against this repo's `node_modules` before this plan was written. Do not swap in a differently-named type from memory.

- Signal Forms (`@angular/forms/signals`, Angular 21.2.6): `form<TModel>(model: WritableSignal<TModel>, schemaOrOptions: SchemaOrSchemaFn<TModel> | FormOptions<TModel>)`, where `SchemaFn<TModel> = (p: SchemaPathTree<TModel>) => void`. The schema-callback parameter type is **`SchemaPathTree<TModel>`** — it is exported from `@angular/forms/signals`. There is no `FieldPath` export in this version.
- ApexCharts (`apexcharts` 5.10.4): the donut total formatter is declared at `node_modules/apexcharts/types/apexcharts.d.ts:996` as `formatter?(w: ApexCharts.ApexOptions & { globals: any }): string`. The `ApexCharts` namespace is globally available with no import.

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Type the Signal Forms schema callback against the login model (DEBT-04)</name>
  <files>src/app/features/login/login.component.ts</files>
  <precondition>`node_modules/` is installed so the `@angular/forms/signals` declaration files are resolvable. This plan runs in Wave 1 alongside plan 01-01, which edits `src/app/core/auth/` and `src/environments/`: `npx tsc --noEmit` compiles the whole app, so if it reports errors *only* in files this plan does not own, that is 01-01's in-flight work — re-run after 01-01 reports done rather than editing those files.</precondition>
  <reversibility rating="reversible">One added interface and two annotations in a single file; reverting is a one-file revert with no runtime or data consequences.</reversibility>
  <read_first>
    - `src/app/features/login/login.component.ts` (lines 149-168 — the model signal, the `form()` call, and the `formValid` computed that reads the resulting field tree)
    - The "Types verified against the installed libraries" table in this plan's `<interfaces>` section
  </read_first>
  <action>
Implements DEBT-04 (GH #9), success criterion 3. Compile-time only: no runtime behavior changes, no new dependency, no template edit.

<!-- planner-discipline-allow: any -->

1. Export an interface `LoginModel { username: string; password: string; }` above the `@Component` decorator, matching the exact shape the model signal already holds.
2. Type the model signal explicitly: `private readonly model = signal<LoginModel>({ username: '', password: '' });`
3. Replace the inline object-literal annotation on the `form()` schema callback parameter at line 160 with `(f: SchemaPathTree<LoginModel>)`, and add `SchemaPathTree` to the existing `@angular/forms/signals` import (which already brings in `form`, `FormField`, `required`).

Use `SchemaPathTree`, not `FieldPath` — `FieldPath` does not exist in `@angular/forms/signals` at Angular 21.2.6. The two `required(...)` calls in the callback body and the `formValid` computed stay exactly as they are.

Do not add a suppression comment or a widening cast, and do not leave a removed annotation behind in a comment — the acceptance gate counts occurrences of the TypeScript top type in this file with comment lines filtered out, and the file must reach zero.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && test "$(grep -v '^\s*//' src/app/features/login/login.component.ts | grep -cE '\bany\b')" -eq 0 && test "$(grep -c 'SchemaPathTree<LoginModel>' src/app/features/login/login.component.ts)" -eq 1 && ! grep -qE '@ts-ignore|@ts-expect-error|as unknown' src/app/features/login/login.component.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/app/features/login/login.component.ts | grep -cE '\bany\b'` returns 0 (baseline before this task: 1, at line 160)
    - `grep -c 'SchemaPathTree<LoginModel>' src/app/features/login/login.component.ts` returns 1
    - `grep -c 'export interface LoginModel' src/app/features/login/login.component.ts` returns 1
    - No suppression or widening escape hatch was used: `grep -nE '@ts-ignore|@ts-expect-error|as unknown' src/app/features/login/login.component.ts` returns no matches
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (see this task's `<precondition>` about concurrent Wave 1 work)
    - `git diff --name-only` lists only `src/app/features/login/login.component.ts` for this task — no `.html` file, no `styles:` array touched
  </acceptance_criteria>
  <done>The schema callback is typed against `LoginModel`, the file contains no top-type annotation, and the project typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Type the ApexCharts donut total formatter (DEBT-05)</name>
  <files>src/app/features/dashboard/dashboard.component.ts</files>
  <read_first>
    - `src/app/features/dashboard/dashboard.component.ts` (lines 27-36 for `PieChartOptions`, and lines 128-147 for the donut `total` block containing the formatter)
    - The "Types verified against the installed libraries" table in this plan's `<interfaces>` section
  </read_first>
  <action>
Implements DEBT-05 (GH #10), success criterion 4. Compile-time only: no runtime behavior changes, no new dependency, no template edit.

<!-- planner-discipline-allow: any -->

1. Export a type alias next to the existing `PieChartOptions` / `LineChartOptions` declarations:
   `export type DonutTotalContext = ApexCharts.ApexOptions & { globals: { seriesTotals: number[] } };`
   The `ApexCharts` namespace is global — add no import for it. This mirrors the library's own declaration at `node_modules/apexcharts/types/apexcharts.d.ts:996`, narrowing only the one `globals` member this formatter actually reads.
2. Annotate the donut total formatter parameter at line 139 with `DonutTotalContext`.
3. Because `seriesTotals` is now a real `number[]`, delete the two now-redundant parameter annotations inside that formatter's `.reduce(...)` callback — the accumulator and element types infer correctly. The returned string and the `toLocaleString('pt-BR', { minimumFractionDigits: 2 })` call stay byte-identical, so the rendered chart label does not change.

Do not add a suppression comment or a widening cast, and do not leave a removed annotation behind in a comment — the acceptance gate counts occurrences of the TypeScript top type in this file with comment lines filtered out, and the file must reach zero.

`dashboard.component.spec.ts` is NOT in this plan's scope — plan 01-02 owns that file. Do not edit it; just keep it passing.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && test "$(grep -v '^\s*//' src/app/features/dashboard/dashboard.component.ts | grep -cE '\bany\b')" -eq 0 && test "$(grep -c 'DonutTotalContext' src/app/features/dashboard/dashboard.component.ts)" -eq 2 && ! grep -qE '@ts-ignore|@ts-expect-error|as unknown' src/app/features/dashboard/dashboard.component.ts && npx ng test --include='**/dashboard.component.spec.ts' --watch=false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/app/features/dashboard/dashboard.component.ts | grep -cE '\bany\b'` returns 0 (baseline before this task: 1, at line 139)
    - `grep -c 'DonutTotalContext' src/app/features/dashboard/dashboard.component.ts` returns 2 (the exported alias plus the annotation)
    - No suppression or widening escape hatch was used: `grep -nE '@ts-ignore|@ts-expect-error|as unknown' src/app/features/dashboard/dashboard.component.ts` returns no matches
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (see Task 1's `<precondition>` about concurrent Wave 1 work)
    - `npx ng test --include='**/dashboard.component.spec.ts' --watch=false` reports 0 failures
    - `git diff --name-only` lists only `src/app/features/dashboard/dashboard.component.ts` for this task — no `.html` file, no `styles:` array touched
  </acceptance_criteria>
  <done>The donut formatter is typed via `DonutTotalContext`, the file contains no top-type annotation, and the pre-existing dashboard spec is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser SPA -> `aque-backend` REST API | `LoginModel` types the credential object the login form submits across this boundary. The shape is unchanged — the interface mirrors what the signal already held — so no field is added to or removed from the request payload. |

The environment files, `angular.json` and the build pipeline are owned by plan 01-01 (threats T-01-01, T-01-02); this plan touches none of them.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `login.component.ts` / `dashboard.component.ts` typing changes | low | accept | Both edits are compile-time only. `LoginModel` mirrors the shape the signal already held and `DonutTotalContext` mirrors the library's own declaration; neither alters a validation rule, a request payload, or rendered output. The two `required(...)` validators in the login schema callback are explicitly left untouched, so the form's input-validation boundary is unchanged. Acceptance criteria forbid `@ts-ignore` / `@ts-expect-error` / `as unknown`, so the strictness cannot be traded away for a passing build. |
| T-01-SC | Tampering | npm installs | high | accept | No package-manager install task exists in this plan — no dependency is added, removed or upgraded, so the supply-chain surface is unchanged and no package legitimacy audit is required. Enforced by acceptance gate: `git diff --exit-code package.json package-lock.json` must exit 0 (see `<verification>`). If a task turns out to need a new dependency, stop and route back to planning for a legitimacy audit rather than installing inline. |

ASVS level 1, block-on threshold `high`. This plan carries no unmitigated `high` threat: T-01-SC is `high` by category but its disposition is `accept` on the grounds that the activity does not occur, with an automated gate proving it.
</threat_model>

<verification>
Run from the repo root after both tasks. Steps 1 and 2 compile and exercise the whole app, so run them once plan 01-01 (the other Wave 1 plan) reports done — an error in `src/app/core/auth/` or `src/environments/` belongs to 01-01, not to this plan.

1. `npm run build` — exits 0.
2. `npm test -- --watch=false` — 0 failures, spec count unchanged. No spec is added, deleted or skipped in this plan.
3. `grep -v '^\s*//' src/app/features/login/login.component.ts | grep -cE '\bany\b'` and the same for `src/app/features/dashboard/dashboard.component.ts` — both return 0.
4. `git diff --exit-code package.json package-lock.json` — exits 0 (T-01-SC gate: no dependency changed).
5. `git diff --name-only` — contains no `.html` file and nothing under `public/` or `src/styles.css`. This plan produces zero rendered-output change.

<human-check>
Optional smoke test, end-of-phase (`workflow.human_verify_mode: end-of-phase`): start the backend, run `npm start`, log in, and load the dashboard. Expected: login succeeds, the required-field validation still blocks an empty form, and the donut chart renders with its `Total` label formatted exactly as before. Nothing on screen should look different — that is the pass condition, since this phase is a pure refactor. Any visible change is a regression, not an improvement.
</human-check>
</verification>

<success_criteria>
Maps to ROADMAP.md Phase 1:

3. The Signal Forms schema callback in `login.component.ts` is typed against `LoginModel` with no top type in its signature — proven by verification step 3.
4. The ApexCharts donut formatter in `dashboard.component.ts` is typed via `DonutTotalContext` with no top type — proven by verification step 3.
5. `npm run build` succeeds and `npm test` passes with the existing 10 specs still green — verification steps 1 and 2.

Closes GH [#9](https://github.com/BrininhoBru/aque-web/issues/9) and [#10](https://github.com/BrininhoBru/aque-web/issues/10).
</success_criteria>

<output>
Create `.planning/phases/01-api-config-type-cleanup/01-03-SUMMARY.md` when done.

Record in the summary: the two new exported type names (`LoginModel`, `DonutTotalContext`) and their exact declared shapes — Phase 3 writes `login.component.spec.ts` against `LoginModel`.
</output>
